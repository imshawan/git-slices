{
  "name": "git-slices",
  "version": "1.0.2",
  "description": "CLI tool to pull specific slices (folders) from a Git repository to specified directories/repository.",
  "main": "src/index.js",
  "type": "module",
  "bin": {
    "git-slices": "bin/pull-slices.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node bin/pull-slices.js",
    "lint": "eslint . --ext .js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": [
    "git",
    "folder",
    "pull",
    "slices",
    "repository",
    "clone",
    "git-slices",
    "CLI"
  ],
  "author": "Shawan Mandal <github@imshawan.dev>",
  "license": "MIT",
  "dependencies": {
    "commander": "^13.1.0",
    "shelljs": "^0.10.0",
    "simple-git": "^3.27.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.23.0",
    "eslint": "^9.23.0",
    "globals": "^16.0.0"
  }
}
